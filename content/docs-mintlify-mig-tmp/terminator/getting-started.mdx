---
title: 'Terminator: Getting Started'
---

## ⚡ hello world — quick start

> skip the boilerplate. this is the fastest way to feel the magic.

first, clone the terminator repository:

```bash
git clone https://github.com/mediar-ai/terminator
cd terminator
```

### 🐍 python

```bash
pip install maturin
cd bindings/python
maturin develop
```

```python
import terminator
desktop = terminator.Desktop()
print(desktop.hello())  # → "Hello from Terminator"
```

### 🟦 typescript / node.js

```bash
cd bindings/nodejs
npm install
npm run build
```

```typescript
const { Desktop } = require('./bindings/nodejs/index.js');
const desktop = new Desktop();
console.log(desktop.hello()); // → "Hello from Terminator"
```

## next steps

*   explore the sdk references ([TypeScript](/terminator/js-sdk-reference), [Python](/terminator/python-sdk-reference)) to understand all available commands.
*   check out the [pdf-to-form example](https://github.com/mediar-ai/terminator/tree/main/examples/pdf-to-form) to see terminator with ai.
*   start building your own automation scripts! 
