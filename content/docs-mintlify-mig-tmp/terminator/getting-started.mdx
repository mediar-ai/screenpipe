---
title: 'Terminator: Getting Started'
---

## installation

first, you need the rust development environment installed.

```bash
# install rust if you don't have it
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $home/.cargo/env # or restart your terminal
```

then, clone the terminator repository:

```bash
git clone https://github.com/mediar-ai/terminator
cd terminator
```

## running the server

**terminator** works with a client-server architecture. you need to run the rust server first. this server listens for commands from the sdk (typescript, python coming soon) and interacts with the operating system's accessibility apis.

```bash
cargo run --example server -- --debug
```

the `--debug` flag enables verbose logging, which is helpful when starting out.

## using the sdks (quick start)

once the server is running, you can use either the typescript or python sdk to automate ui interactions.

### typescript

1.  **navigate to the examples directory:**
    ```bash
    cd examples
    ```
2.  **install dependencies (if needed):**
    ```bash
    npm i # or bun install / yarn install
    # this might install the local ts-sdk if linked in package.json
    ```
3.  **run the ts example script:**
    ```bash
    npx tsx client_example.ts
    ```

*see the [TypeScript SDK Reference](/terminator/js-sdk-reference) for details.*

### python

1.  **navigate to the examples directory:**
    ```bash
    cd examples
    ```
2.  **run the python example script:**
    ```bash
    python client_example.py
    ```

*see the [Python SDK Reference](/terminator/python-sdk-reference) for details.*

## next steps

*   explore the sdk references ([TypeScript](/terminator/js-sdk-reference), [Python](/terminator/python-sdk-reference)) to understand all available commands.
*   look at the `examples/` directory for more usage patterns.
*   start building your own automation scripts! 
