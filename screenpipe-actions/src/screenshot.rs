// use anyhow::Result;
// use image::DynamicImage;
// use screenshots::Screen;

// Commented out unused function
// pub fn capture_main_window_screenshot() -> Result<DynamicImage> {
//     // ... existing code ...
// }

// Commented out unused function
// fn is_valid_window(window: &Window) -> bool {
//     let title = window.title().unwrap_or_default();
//     let class = window.class().unwrap_or_default();
//     
//     !title.is_empty() && !class.is_empty() && title != "Desktop" && class != "Desktop"
// }

// If there are any other functions or code in this file, keep them as is
